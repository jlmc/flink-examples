# Hello world 

Important to notice that to run or debug in the idea it is necessary add all the provider runtime dependencies to the classpath.

In IDEA, modify the run configuration:
![](imgs/img.png)